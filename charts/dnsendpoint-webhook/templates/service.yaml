apiVersion: v1
kind: Service
metadata:
  name: {{ include "dnsendpoint-webhook.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "dnsendpoint-webhook.name" . }}
    chart: {{ include "dnsendpoint-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "dnsendpoint-webhook.name" . }}
    release: {{ .Release.Name }}
