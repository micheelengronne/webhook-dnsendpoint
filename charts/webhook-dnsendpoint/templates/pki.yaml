---
# Create a selfsigned Issuer, in order to create a root CA certificate for
# signing webhook serving certificates
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "webhook-dnsendpoint.selfSignedIssuer" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "webhook-dnsendpoint.name" . }}
    chart: {{ include "webhook-dnsendpoint.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  selfSigned: {}

---

# Generate a CA Certificate used to sign certificates for the webhook
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "webhook-dnsendpoint.rootCACertificate" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "webhook-dnsendpoint.name" . }}
    chart: {{ include "webhook-dnsendpoint.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  secretName: {{ include "webhook-dnsendpoint.rootCACertificate" . }}
  duration: 43800h # 5y
  issuerRef:
    name: {{ include "webhook-dnsendpoint.selfSignedIssuer" . }}
  commonName: "ca.webhook-dnsendpoint.cert-manager"
  isCA: true

---

# Create an Issuer that uses the above generated CA certificate to issue certs
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "webhook-dnsendpoint.rootCAIssuer" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "webhook-dnsendpoint.name" . }}
    chart: {{ include "webhook-dnsendpoint.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ca:
    secretName: {{ include "webhook-dnsendpoint.rootCACertificate" . }}

---

# Finally, generate a serving certificate for the webhook to use
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "webhook-dnsendpoint.servingCertificate" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "webhook-dnsendpoint.name" . }}
    chart: {{ include "webhook-dnsendpoint.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  secretName: {{ include "webhook-dnsendpoint.servingCertificate" . }}
  duration: 8760h # 1y
  issuerRef:
    name: {{ include "webhook-dnsendpoint.rootCAIssuer" . }}
  dnsNames:
  - {{ include "webhook-dnsendpoint.fullname" . }}
  - {{ include "webhook-dnsendpoint.fullname" . }}.{{ .Release.Namespace }}
  - {{ include "webhook-dnsendpoint.fullname" . }}.{{ .Release.Namespace }}.svc
