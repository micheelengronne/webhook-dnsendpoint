---
# Create a selfsigned Issuer, in order to create a root CA certificate for
# signing webhook serving certificates
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "dnsendpoint-webhook.selfSignedIssuer" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "dnsendpoint-webhook.name" . }}
    chart: {{ include "dnsendpoint-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  selfSigned: {}

---

# Generate a CA Certificate used to sign certificates for the webhook
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "dnsendpoint-webhook.rootCACertificate" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "dnsendpoint-webhook.name" . }}
    chart: {{ include "dnsendpoint-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  secretName: {{ include "dnsendpoint-webhook.rootCACertificate" . }}
  duration: 43800h # 5y
  issuerRef:
    name: {{ include "dnsendpoint-webhook.selfSignedIssuer" . }}
  commonName: "ca.dnsendpoint-webhook.cert-manager"
  isCA: true

---

# Create an Issuer that uses the above generated CA certificate to issue certs
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "dnsendpoint-webhook.rootCAIssuer" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "dnsendpoint-webhook.name" . }}
    chart: {{ include "dnsendpoint-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ca:
    secretName: {{ include "dnsendpoint-webhook.rootCACertificate" . }}

---

# Finally, generate a serving certificate for the webhook to use
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "dnsendpoint-webhook.servingCertificate" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "dnsendpoint-webhook.name" . }}
    chart: {{ include "dnsendpoint-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  secretName: {{ include "dnsendpoint-webhook.servingCertificate" . }}
  duration: 8760h # 1y
  issuerRef:
    name: {{ include "dnsendpoint-webhook.rootCAIssuer" . }}
  dnsNames:
  - {{ include "dnsendpoint-webhook.fullname" . }}
  - {{ include "dnsendpoint-webhook.fullname" . }}.{{ .Release.Namespace }}
  - {{ include "dnsendpoint-webhook.fullname" . }}.{{ .Release.Namespace }}.svc
